<h1>Your Cart</h1>

<ul>
  <% @cart.cart_items.each do |item| %>
    <li>
      <% if item.product %>
        <%= link_to item.product.name, product_path(item.product) %>
      <% elsif item.part %>
        <%= link_to item.part.name, part_path(item.part) %>
      <% end %>
      - Quantity: <%= item.quantity %>
    </li>
  <% end %>
</ul>

<%= link_to 'Checkout', new_order_path %>
