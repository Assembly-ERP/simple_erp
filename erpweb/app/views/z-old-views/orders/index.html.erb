<h1>Your Orders</h1>

<% if @orders.any? %>
  <ul>
    <% @orders.each do |order| %>
      <li>
        <%= link_to "Order ##{order.id}", order_path(order) %> - <%= order.status %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>You do not have any orders yet.</p>
<% end %>
